module.exports = (config) => {
  let configuration = {
  
    browsers: ['ChromeDocker','FirefoxHeadless'],
    customLaunchers: {
      ChromeDocker: {
        base: 'ChromeHeadless',
        flags: [
           '--no-sandbox',
           '--disable-web-security'
        ]
      }
    },

    reporters: ['dots', 'junit'],
    junitReporter: {
      outputFile: 'test-results.xml'
    },
  };
  config.set(configuration);
};

